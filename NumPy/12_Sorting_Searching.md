# Sorting and Searching

## Sorting

```python
import numpy as np

arr = np.array([40,10,30,20])
print(np.sort(arr))
```

## Searching

```python
print(np.where(arr == 30))
```

## Practice
Sort marks and find index of highest value.